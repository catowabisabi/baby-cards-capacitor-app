import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mono12studio.babycards',
  appName: '文文認字',
  webDir: 'dist',
  backgroundColor: '#fff7ed',
};

export default config;
